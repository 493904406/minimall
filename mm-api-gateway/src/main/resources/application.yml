eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8769
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /ribbon/**
      serviceId: ribbon-consumer
    api-b:
      path: /user/**
      serviceId: user-consumer
